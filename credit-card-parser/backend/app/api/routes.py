"""
API routes (optional - currently all routes in main.py)
Can be used to organize routes if the application grows
"""
from fastapi import APIRouter

router = APIRouter()

# Future routes can be added here